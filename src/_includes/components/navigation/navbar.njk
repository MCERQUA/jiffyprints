{# Component: Navigation Bar #}
{# Description: Main site navigation with logo and menu #}
{# Props: current_page #}
{# Variants: desktop, mobile #}
{# Lines: 60+ #}

{% set current = page.url %}

<nav class="navbar">
    <div class="container nav-content">
        <a href="/" class="logo">{{ site.title }}</a>
        
        <!-- Desktop Navigation -->
        <ul class="nav-links">
            {% for item in navigation.primary %}
            <li>
                <a href="{{ item.url }}" 
                   class="nav-link{% if current == item.url %} nav-link-active{% endif %}">
                    {{ item.text }}
                </a>
            </li>
            {% endfor %}
        </ul>
        
        <!-- Mobile: Phone Number and CTA + Menu -->
        <div class="mobile-header-right">
            <a href="tel:{{ site.phone }}" class="mobile-phone">{{ site.phone }}</a>
            <a href="{{ navigation.cta.url }}" class="cta-btn">Quote</a>
            <button class="mobile-menu-toggle" aria-label="Toggle mobile menu">
                <span></span>
                <span></span>
                <span></span>
            </button>
        </div>
        
        <!-- Desktop CTA Button -->
        <a href="{{ navigation.cta.url }}" class="cta-btn desktop-only">Quote</a>
    </div>
    
    <!-- Mobile Navigation Menu -->
    <div class="mobile-menu" id="mobileMenu">
        <ul class="mobile-nav-links">
            {% for item in navigation.primary %}
            <li>
                <a href="{{ item.url }}" 
                   class="mobile-nav-link{% if current == item.url %} mobile-nav-link-active{% endif %}">
                    {{ item.text }}
                </a>
            </li>
            {% endfor %}
        </ul>
    </div>
</nav>